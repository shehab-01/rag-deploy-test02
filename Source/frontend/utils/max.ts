/**
 * max 공통스크립트
 */
import ui from '@/common/max.ui';
import util from '@/common/max.util';
// import grid from '@/common/max.grid';
import storage from '@/common/max.storage';

const max = {
  ui: ui,
  util: util,
  // grid: grid,
  storage: storage
}; // prettier-ignore

export default max;
